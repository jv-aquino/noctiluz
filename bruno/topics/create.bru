meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/{{folder}}
  body: json
  auth: none
}

body:json {
  {
    "name": "Novo Tópico",
    "description": "Descrição do tópico",
    "slug": "novo-topico",
    "subjectId": "{{subjectId}}"
  }
}

script:post-response {
  if (res.body && res.body.id) {
    bru.setVar("topicId", res.body.id);
  } else {
    console.log("No ID found in response");
  }
}
