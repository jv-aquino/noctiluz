meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/curso
  body: json
  auth: none
}

body:json {
  {
    "name": "Nova Curso",
    "descricao": "Descriçã2 do curso",
    "slug": "novo-curso"
  }
}

script:post-response {
  if (res.body && res.body.id) {
    bru.setVar("cursoId", res.body.id);
  } else {
    console.log("No ID found in response");
  }
}
