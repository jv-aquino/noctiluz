meta {
  name: getAll
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/{{folder}}
  body: none
  auth: none
}

script:post-response {
  if (res.body && res.body[0].id) {
    bru.setVar("lessonId", res.body[0].id);
  } else {
    console.log("No ID found in response");
  }
}
