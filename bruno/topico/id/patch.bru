meta {
  name: patch
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/topico/{{topicoId}}
  body: json
  auth: inherit
}

headers {
  Cookie: better-auth.session_token={{sessionValue}}
}

body:json {
  {
    "name": "Tópico Atualizado",
    "descricao": "Descrição atualizada do tópico"
  }
}
